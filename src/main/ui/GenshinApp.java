package ui;

import model.Team;
import model.TeamList;

// Genshin Team Tracker Application
public class GenshinApp {
    public GenshinApp() {
        TeamList teams = new TeamList();
    }
    // Create and add multiple teams to a list of teams
    // teams.addTeam();

    // Add up to four characters to a team
    // teams.getTeam(1).addCharacter("Keqing");

    // Know what elements are on a team
    // displayElements().teams.getTeam(2).

    // Know what elemental resonance(s) is/are present on a team
    // displayElementalResonance.teams.getTeam(3)


    public void displayElements(Team team) {
        // STUB
    }

    public void displayElementalResonance(Team team) {
        // STUB
    }
}
